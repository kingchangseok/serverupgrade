
# ┌──────┬───────────────────────┐
# │ 프로그램명 │ Makefile                                     │
# ├──────┼───────────────────────┤
# │ 기      능 │ UNIX c Compile Script                        │
# ├──────┼───────────────────────┤
# │ 작  성  일 │ 2007. 10. 24                                 │
# ├──────┼───────────────────────┤
# │ 작  성  자 │ 최   병   남                                 │
# └──────┴───────────────────────┘

CFLAGS=-DLOGGING -g

AR=ar rc
#CC=cc -O2 -D MULTIARG -D TEST_MODE -D ON_LINE -D LOGGING
CCcurses=cc
eCAMS_HOME=$(APPHOME)
INCLUDE= $(eCAMS_HOME)/inc
LIBcurses=-lcurses -ltermcap
BINPATH=$(eCAMS_HOME)/bin
LIBPATH=$(eCAMS_HOME)/lib
TARGET =$(LIBPATH)/libecams_comm32.a
OBJECT_MODE=32

CC=cc -D MULTIARG -D ON_LINE -D LOGGING -I$(INCLUDE)

ENCLIB= des.o 		\
		des3.o 		\
		des3enc.o	\
		encrypt.o

all: libenc

.SUFFIXES: .c .o

clean:
	rm -f *.o core

.c.o:
	$(CC)  -c $*.c -o $@ -I$(INCLUDE)

libenc: $(ENCLIB)
	$(AR) $(TARGET) $(ENCLIB)
	rm *.o

